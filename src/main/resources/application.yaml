server:
  servlet:
    session:
      cookie:
        secure: true       # т.к. github.io всегда https
        same-site: none    # иначе браузер заблокирует куку

spring:
  application:
    name: habitus
  security:
    user:
      name: admin
      password: admin
  datasource:
    url: jdbc:postgresql://postgres:5432/postgres
    username: habitus_user
    password: habitus_pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org:
      springframework:
        security: WARN
        web.filter.CommonsRequestLoggingFilter: DEBUG

springdoc:
  swagger-ui:
#    disable-swagger-default-url: true
    operationsSorter: method     # или alpha, method, none
    tagsSorter: alpha            # или alpha, none
